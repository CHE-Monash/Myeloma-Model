#File types to ignore
.DS_Store
**/.DS_Store
*.log
*.smcl
*~.do
*.gph
*.bak
*.tmp

# Additional macOS system files
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Windows system files (if collaborating with Windows users)
Thumbs.db
Desktop.ini
ehthumbs.db

# Stata-specific temporary files
*.dta~
*.do~
*.ado~

# Simulated data - CORRECTED PATTERN
# First, don't ignore the simulated directories themselves
!analyses/*/data/simulated/
# Then ignore all contents within them
analyses/*/data/simulated/*
# Finally, un-ignore the .gitkeep files
!analyses/*/data/simulated/.gitkeep
